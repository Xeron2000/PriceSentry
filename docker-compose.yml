services:
  backend:
    image: ${PRICESENTRY_BACKEND_IMAGE:-ghcr.io/xeron2000/pricesentry-backend:latest}
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "18000:8000"
    volumes:
      - ./config:/app/config
      - ./data:/app/data

  dashboard:
    image: ${PRICESENTRY_DASHBOARD_IMAGE:-ghcr.io/xeron2000/pricesentry-dashboard:latest}
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - backend
    ports:
      - "13000:3000"
