# PriceSentry 价格监控系统配置模板
# =====================================

# 主要交易所（必填）
# 可选值: "binance", "okx", "bybit"
exchange: "okx"

# 默认时间周期（K 线聚合窗口）
# 可选值: "1m", "5m", "15m", "1h", "1d"
defaultTimeframe: "5m"

# 监控任务检查频率（调度间隔，未配置时回退到 defaultTimeframe）
checkInterval: "1m"

# 价格变化阈值（百分比）
defaultThreshold: 1

# 通知渠道（目前仅支持 telegram）
notificationChannels:
  - "telegram"

# 通知交易对
# 💡 提示：
#   - 输入 'default' 使用市值前50币种（推荐新手使用）
#   - 或手动指定交易对列表
#   - 留空表示推送所有监控到的合约交易对
#
# 示例1 - 使用默认市值前50（推荐）:
# notificationSymbols: default
#
# 示例2 - 手动指定:
notificationSymbols:
  - "BTC/USDT:USDT"
  - "ETH/USDT:USDT"
  - "SOL/USDT:USDT"
#
# 市值前50币种包括:
# BTC, ETH, BNB, XRP, SOL, USDC, DOGE, ADA, TRX, LINK,
# AVAX, XLM, BCH, DOT, SHIB, SUI, HBAR, LTC, UNI, NEAR,
# PEPE, APT, ICP, POL, FIL, ARB, VET, ETC, ATOM, OP,
# INJ, MNT, CRO, IMX, STX, OKB, KAS, RENDER, SEI, TIA,
# BONK, FTM, GRT, RUNE, ALGO, FLOKI

# Telegram 机器人配置（至少提供 token，chatId 用作回退通道）
telegram:
  token: "YOUR_TELEGRAM_BOT_TOKEN"
  chatId: "123456789"
  # webhookSecret: "YOUR_WEBHOOK_SECRET"  # 可选，使用 Webhook 时填写

# 通知时区（用于生成告警时间戳）
notificationTimezone: "Asia/Shanghai"

# =====================================
# 可选功能
# =====================================

# 是否在通知中附带价格图表
attachChart: true

# 图表渲染设置（attachChart 为 true 时生效）
chartTimeframe: "5m"
chartLookbackMinutes: 500
chartTheme: "dark"          # "dark" | "light"
chartIncludeMA: [7, 25]
chartImageWidth: 1600
chartImageHeight: 1200
chartImageScale: 2

# 额外说明
# - 默认会监控支持的 USDT 合约交易对，notificationSymbols 为空或缺省时会推送全部交易对。
# - 缓存系统与错误处理在代码中默认启用，无需单独配置。
